# MotionScan

## Shared API Contract
`packages/shared-contract/screening-schema.json` is the single source of truth for what a
screening record looks like. Check here before changing what the Field App writes or the
Dashboard reads — do not let the two drift apart independently.